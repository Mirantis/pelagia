---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: cephdeploymentmaintenances.lcm.mirantis.com
spec:
  group: lcm.mirantis.com
  names:
    kind: CephDeploymentMaintenance
    listKind: CephDeploymentMaintenanceList
    plural: cephdeploymentmaintenances
    shortNames:
    - cephdplmnt
    singular: cephdeploymentmaintenance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: Message for current state
      jsonPath: .status.message
      name: Message
      type: string
    - description: Current maintenance state
      jsonPath: .status.state
      name: State
      type: string
    - description: Last state check time
      jsonPath: .status.lastStateCheck
      name: Last check
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CephDeploymentMaintenance aggregates info about miraceph maintenance
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          status:
            description: Status contains overall status info for CephDeploymentMaintenance
            properties:
              lastStateCheck:
                description: last state check timestamp
                type: string
              message:
                description: message for current state
                nullable: true
                type: string
              state:
                description: current state of maintenance
                type: string
            required:
            - lastStateCheck
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
