name: Build CI

on:
  push:
    branches: [ "main", "release-*" ]
    paths:
    - 'cmd/**'
    - 'pkg/**'
    - 'go.mod'
    - 'controller.Dockerfile'
    - 'Makefile'
  pull_request:
    branches: [ "main", "release-*" ]
    paths:
    - 'cmd/**'
    - 'pkg/**'
    - 'go.mod'
    - 'controller.Dockerfile'
    - 'Makefile'

# abort in progress when PR is refreshed.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.head_ref || github.sha }}
  cancel-in-progress: true

jobs:
  build-controller:
    runs-on: ubuntu-latest
    env:
      BUILD_MODE: ${{ github.event_name == 'push' && 'merge' || 'dev' }}
      IMAGE_NAME: ${{ github.event_name == 'pull_request' && format('{0}/pelagia/pr/pelagia', vars.REGISTRY_CI) || format('{0}/pelagia/pelagia', vars.REGISTRY_CI) }}
      DEV_VERSION: ${{ github.event_name == 'pull_request' && github.event.pull_request.number }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23.6'
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all
      - name: Check lint
        run: make lint
      - name: Show current version
        run: make get-version
      - name: Run image build
        run: make image
      - name: Login to registry CI
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.REGISTRY_CI }}
          username: ${{ secrets.REGISTRY_CI_USERNAME }}
          password: ${{ secrets.REGISTRY_CI_PASSWORD }}
      - name: Push build
        run: make publish
